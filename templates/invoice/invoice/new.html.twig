{% extends 'base.html.twig' %}{% block title %}Nueva Factura{% endblock %}{% block body %}
<a href="{{ path('app_invoice_index') }}" class="btn btn-sm btn-light rounded-pill mb-3"><i class="bi bi-arrow-left"></i> Facturas</a>
<h3 class="fw-bold mb-3">Nueva factura</h3><div class="card-modern p-4"><div class="form-modern">{{ form_start(form) }}{{ form_widget(form) }}<div class="mt-4"><button class="btn btn-indigo">Guardar</button></div>{{ form_end(form) }}</div></div>{% endblock %}

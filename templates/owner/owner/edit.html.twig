{% extends 'base.html.twig' %}{% block title %}Editar {{ owner.nombre }}{% endblock %}{% block body %}
<a href="{{ path('app_owner_show', {'id': owner.id}) }}" class="btn btn-sm btn-light rounded-pill mb-3"><i class="bi bi-arrow-left"></i> Volver</a>
<h3 class="fw-bold mb-4">Editar propietario</h3>
<div class="card-modern p-4 p-md-5"><div class="form-modern">{{ form_start(form) }}{{ form_widget(form) }}<div class="mt-4"><button class="btn btn-indigo px-4">Actualizar</button></div>{{ form_end(form) }}</div></div>
{% endblock %}

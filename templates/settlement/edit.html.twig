{% extends 'base.html.twig' %}
{% block title %}Editar Liquidación #{{ settlement.id }}{% endblock %}
{% block body %}
<div class="container py-4" style="max-width:720px">
  <a href="{{ path('app_settlement_index') }}" class="btn btn-light btn-sm mb-3"><i class="bi bi-arrow-left"></i> Volver</a>
  <h5 class="mb-3">Editar Liquidación #{{ settlement.id }} <span class="badge" style="background:#eef2ff;color:#4338ca">{{ settlement.estado }}</span></h5>
  {{ include('settlement/_form_edit.html.twig', {'form': form}) }}
</div>
{% endblock %}
